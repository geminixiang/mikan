name: Publish to npm

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22.19.0"
          registry-url: "https://registry.npmjs.org"

      - name: Update npm
        run: npm install -g npm@latest

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Test
        run: npm test

      - name: Publish to npm
        run: |
          VERSION=$(node -p "require('./packages/mikan/package.json').version")
          if [[ "$VERSION" == *-* ]]; then
            npm publish --workspace @geminixiang/mikan --provenance --access public --tag beta
          else
            npm publish --workspace @geminixiang/mikan --provenance --access public
          fi
