store-dir=.pnpm-store
# npm-style flat node_modules: the daemon's type graph (e.g. @sentry/core's
# Integration type) was written against npm hoisting; strict pnpm isolation
# breaks "cannot be named" type references. Web packages resolve through Vite
# aliases regardless.
shamefully-hoist=true
