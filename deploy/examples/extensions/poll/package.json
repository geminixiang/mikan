{
  "name": "poll",
  "version": "0.1.0",
  "private": true,
  "description": "Slack poll via api.blockkit: /poll command, button voting, live tally — zero LLM calls",
  "type": "module",
  "devDependencies": {
    "@geminixiang/mikan": "*"
  },
  "mikan": {
    "extensions": [
      "./index.ts"
    ]
  }
}
