/**
 * @geminixiang/mikan-daemon-web-bridge — types and seams shared between the
 * mikan daemon (src/) and the web surface. Skeleton package: the wire types
 * land in a follow-up change.
 */

/** Placeholder identity so the package compiles; replaced by the wire types. */
export const PACKAGE_NAME = "@geminixiang/mikan-daemon-web-bridge";
