/**
 * @geminixiang/mikan-web-bundle — web composition. Skeleton package: the boot
 * graph builder lands in a follow-up change.
 */

/** Placeholder identity so the package compiles; replaced by the boot graph. */
export const PACKAGE_NAME = "@geminixiang/mikan-web-bundle";
