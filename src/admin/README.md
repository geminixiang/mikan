# src/admin

This directory provides the admin portal and admin token storage.

## Files

- `portal.ts`: Implements the `/admin` Web UI/API for conversations, models, sandbox, auto-reply, workspace, skills, events, and links.
- `store.ts`: Provides a short-lived in-memory admin token store.
