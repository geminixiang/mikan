---
title: デプロイ
description: 永続 state、managed sandbox images、graceful shutdown、任意の health endpoint を使い、PM2 で mikan を実行します。
---

import { Aside, Card, CardGrid, Steps } from "@astrojs/starlight/components";

<CardGrid>
  <Card title="PM2 supervisor" icon="setting">
    PM2 は mikan を daemonize し、障害後に再起動し、stdout/stderr logs を保持し、boot
    時に起動できます。
  </Card>
  <Card title="Managed image sandbox" icon="rocket">
    mikan host process は Docker を使い、選択した sandbox image から conversation ごとの tool
    containers を作成します。
  </Card>
  <Card title="永続 state" icon="lock">
    settings、session に依存しない runtime state、vaults、GitHub polling watermarks 用に、host-only
    の state directory を 1 つ保持します。
  </Card>
</CardGrid>

## 前提条件

- Node.js `>=22.19.0`
- 少なくとも 1 組の完全な Slack、Telegram、Discord、または GitHub credentials
- 永続的な workspace directory
- `image:*` または `container:*` 使用時に PM2 user が利用できる Docker

公開されている `mikan-sandbox` image は tool runtime であり、standalone mikan server image ではありません。PM2 は host 上で `mikan` を実行し、image mode は host Docker daemon を通じて containers を作成、管理します。

## PM2 デプロイ

<Steps>

1. mikan と PM2 をインストールします：

   ```bash
   npm i -g @geminixiang/mikan pm2
   ```

2. service が使用する正確な state directory にグローバル設定ファイルを作成します：

   ```bash
   mkdir -p /srv/mikan/workspace
   mikan --onboard --state-dir=/srv/mikan/state
   ```

   `/srv/mikan/state/settings.json` を確認してください。directory は PM2 process user が所有し、
   world-writable でない必要があります。`chmod 0700 /srv/mikan/state` を推奨します。

3. managed sandbox image を pull します：

   ```bash
   docker pull ghcr.io/geminixiang/mikan-sandbox:latest
   ```

   選択した mode が `image:*` の場合、長時間稼働する container を別途起動しないでください。既存の
   container が必要なのは `container:<name>` mode だけです。

4. ecosystem file をダウンロードします：

   ```bash
   curl -O https://raw.githubusercontent.com/geminixiang/mikan/main/deploy/pm2/ecosystem.config.cjs
   ```

5. `args` を編集し、絶対 paths と 1 つの sandbox mode を指定します：

   ```js
   args: "--state-dir=/srv/mikan/state --sandbox=image:ghcr.io/geminixiang/mikan-sandbox:latest /srv/mikan/workspace",
   ```

   次に、PM2 の環境に少なくとも 1 組の完全な platform credentials を設定します。可能なら private keys
   と tokens は ecosystem file に含めず、service manager または secret store から読み込んでください。

6. process を起動し、永続化します：

   ```bash
   pm2 start ecosystem.config.cjs
   pm2 save
   pm2 startup
   ```

7. `pm2 startup` が出力したコマンドを実行し、boot 時の自動起動を有効にします。

</Steps>

相対 workspace paths は PM2 の current working directory から解決されます。絶対 paths を使うと、service の移行や reboot 後に別の workspace を暗黙に開くことを防げます。

## アップグレード

```bash
npm i -g @geminixiang/mikan
pm2 reload mikan
```

<Aside type="note" title="Graceful reload">
  同梱の PM2 設定は SIGTERM 後、SIGKILL まで最大 60 秒待機します。mikan は最初に conversation
  runtime を drain し、events watcher を停止し、Sentry の flush を最大 5 秒待ちます。supervisor
  timeout を超える runs は、それでも終了される場合があります。
</Aside>

## Health endpoint

portal server は `LINK_PORT` が設定されている場合、または `LINK_URL` によって既定 port `8181` が使われる場合にのみ存在します：

```bash
curl http://127.0.0.1:8181/health
# {"ok":true}
```

`/health` は HTTP process の liveness check にすぎません。platform connections、Docker、sandbox provisioning、LLM providers、event delivery は検証しないため、唯一の readiness signal として使用しないでください。

mode 固有の要件は [GitHub で保守されている PM2 ecosystem file](https://github.com/geminixiang/mikan/blob/main/deploy/pm2/ecosystem.config.cjs) と [Sandbox modes](sandbox/) を参照してください。
