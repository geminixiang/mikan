---
title: Sandbox
---

import { Aside, Badge, Card, CardGrid, LinkCard } from "@astrojs/starlight/components";

<CardGrid>
  <Card title="ローカル開発" icon="setting">
    `host` は設定が最小で、vault env を注入しません。すべての office が一つの filesystem を共有するため、
    private office の visibility を強制できない trusted deployment です。
  </Card>
  <Card title="主流の隔離" icon="rocket">
    `image:<image>` は mikan が lifecycle、workspace mount、vault env、リソース制限を管理します。
  </Card>
</CardGrid>

<Aside type="caution" title="モード名">
  `docker:*` は利用可能なモードではありません。`container:*` または `image:*` を使ってください。
</Aside>

## 対応モード

| モード                    | 実行場所              | Vault env injection | Vault key                        | 備考                                                                                                 |
| ------------------------- | --------------------- | ------------------- | -------------------------------- | ---------------------------------------------------------------------------------------------------- |
| `host`                    | ホストマシン          | 注入しない          | プラットフォームの user から導出 | ローカル開発専用。office visibility を強制できない                                                   |
| `container:<name>`        | 既存 Docker container | 注入                | container 名から導出             | one container one vault。複数人が同じ container を共有すると、その vault も共有します                |
| `image:<image>`           | mikan 管理の Docker   | 注入                | office key                       | 現在もっとも推奨される隔離モード。`1 conversation = 1 vault = 1 container`                           |
| `cloudflare:<sandbox-id>` | Cloudflare Worker     | 注入                | office key                       | 構築中。`@cloudflare/sandbox` bridge を自分でデプロイする必要があり、host workspace は同期されません |

office key は、workspace 配下でその conversation の directory も命名する、バージョン付きの
`v1-<platform>-<readable-id>-<hash>` セグメントです。[Conversation office](#conversation-office) を
参照してください。

## Conversation office

各 conversation は workspace root 配下に 1 つの directory — その _office_ — を所有します。この
directory はプラットフォームの生の conversation id ではなく office key で命名されます。sandbox の
mount はこの directory に従うため、runtime の中では office は `/workspace/<office-key>` にあります。

runtime が workspace のどの部分を見るかは、その office の **visibility**（ADR 0008）に従います。visibility は Slack の conversation type から導出されます。public channel は public、private channel・DM・group DM・外部共有 channel・未知の種別は private です。すべての office は同じ mount 形状を持ち、違うのは読み取り専用フラグだけです：

| `/workspace` 配下に mount されるもの            | Public office | Private office |
| ----------------------------------------------- | ------------- | -------------- |
| `<office-key>/` — office 自身                   | 読み書き      | 読み書き       |
| `MEMORY.md`、`skills/` — 共有知識               | 読み書き      | 読み取り専用   |
| `public/<office-key>/` — すべての public office | 読み取り専用  | 読み取り専用   |

workspace root を mount する layout はありません。admin portal または `/pi-sandbox visibility private` で public channel を private に狭められますが、Slack の設定より広げることはできません。visibility を強制できるのは `image:*` だけです。他のモードは `managedProjection: false` を報告し、private office を一度だけ警告を記録して通常どおり動かします。フィールドの意味と廃止された door policy 設定は [設定](/ja/configuration/) を参照してください。

<Aside type="caution" title="visibility は追加の sandbox ではありません">
  visibility が決めるのは、どの host directory が runtime に投影されるかです。実行の境界では
  ありません。`host` mode は office の visibility
  にかかわらず、ファイルシステムやプロセスの隔離なしに host 上で直接ツールを実行します。
</Aside>

### 生 id 配置からのアップグレード

office 配置より前に作成された workspace には、生の conversation id で命名された directory が
含まれます。起動のたびにそれらは移行されます — workspace directory、conversation の vault key、
conversation ごとの host state — そして各移動が journal されるため、中断しても conversation を
失わずに再開できます。

推測せず、意図的に boot を止める状況が 2 つあります：

- **所有者不明の directory。** 複数のプラットフォームが有効な場合、mikan はどれが生 directory の
  所有者かを判定できません。`mikan office claim <conversationId> <platform>` で所有者を指定し
  （daemon は停止した状態で）、次回起動時に移動が実行されます。
- **衝突**、つまり legacy と office key の両方の directory がすでに存在する場合。これらは手作業での
  統合のために報告され、上書きされることはありません。

管理下の container は rename をまたいで生き残ります。bind は実行中の container の snapshot に対して
変換されるため、writable layer は base image から作り直されるのではなく保持されます。

## 各モードのドキュメント

<LinkCard
  title="Host sandbox"
  description="ホストマシンで直接ツールを実行します。ローカル開発に最適です。"
  href="host/"
/>
<LinkCard
  title="Container sandbox"
  description="既存の Docker container に接続し、ユーザー管理の lifecycle をそのまま使います。"
  href="container/"
/>
<LinkCard
  title="Image sandbox"
  description="mikan が per-conversation container とリソース制限を管理します。"
  href="image/"
/>
<LinkCard />
<LinkCard />
<LinkCard
  title="Cloudflare sandbox"
  description="Cloudflare Worker bridge 経由でツールを実行します。構築中です。"
  href="cloudflare/"
/>

## 機能差分

`image:<image>` <Badge text="推奨" variant="success" /> は現在の主要開発および推奨 sandbox モードです。他のモードはローカル開発、互換、または実験用途として残されており、一部機能は補完されません。

| ------------------------------------ | ------------ | ------------------- | --------------- | --------------------- | ------------------- | -------------- |
| command 実行 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| mikan 管理の runtime lifecycle | 対象外 | ❌ | ✅ | ✅ mikan process 所有 | ❌ | ❌ |
| per-conversation container / runtime | ❌ | ❌ | ✅ | ✅ | 自分で管理 | bridge 派生 id |
| per-conversation vault env | ❌ | ❌ | ✅ | ✅ | ✅ | ✅ |
| vault file 自動投影 / bind mount | ❌ | ❌ | ✅ | ✅ | ❌ | ❌ |
| workspace 自動マウント | host | 自分で mount が必要 | ✅ | ✅ | 自分で mount が必要 | ❌ |
| office visibility の強制 | ❌ | ❌ | ✅ | ✅ | ❌ | ❌ |
| idle auto-stop / recreate | 対象外 | ❌ | ✅ | ✅ | ❌ | ❌ |
| CPU / memory default limits | ❌ | ❌ | ✅ | ✅ | ❌ | ❌ |
| `/pi-sandbox boost` | ❌ | ❌ | ✅ | ✅ | ❌ | ❌ |
| agent `sandbox` tool 設定 limits | ❌ | ❌ | ✅ | ✅ | ❌ | ❌ |
| 推奨度 | ローカル開発 | legacy / 互換 | 主流 | preview | alpha | 構築中 |

上の ❌ の行の多くは、黙って機能を落とすのではなく拒否します。例外は office visibility で、強制できない
モードは office ごとに一度 `Sandbox '<type>' cannot enforce private office visibility` を記録して
続行します。これらのモードは定義上 trusted deployment だからです。vault file を mount
できないモードは、認証情報なしで実行する代わりに
`Sandbox type "<type>" does not support vault file mounts` を送出します。ファイルを投影できない
モードでは、vault の認証情報は `env` のみに留めてください。
