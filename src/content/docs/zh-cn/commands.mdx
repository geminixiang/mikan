---
title: 聊天命令
description: mikan 在各聊天平台支持的文本命令和 slash-command 别名。
---

import { Aside, Badge, Card, CardGrid, Steps } from "@astrojs/starlight/components";

<CardGrid>
  <Card title="登录和凭证" icon="lock">
    `/login` 打开一个有效期 15 分钟的 capability 链接，用于 API key、OAuth 和共享 vault
    配置文件。它仅在 DM/私聊中可用。
  </Card>
  <Card title="会话" icon="open-book">
    `session` 打开当前会话查看器；`/new` 重置当前 DM 会话，同时保留旧文件供检查。
  </Card>
  <Card title="管理" icon="setting">
    `/admin` 打开有效期 30 分钟的管理
    portal；可以在其中检查模型、沙箱限制、办公室门禁策略、回复策略、技能、事件和对话状态。
  </Card>
</CardGrid>

## 命令速查

| 命令                                                 | 用途和范围                                                                                    |
| ---------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| `/login` / `/pi-login`                               | 打开有效期 15 分钟的登录/vault 链接。仅限 DM。                                                |
| `session` / `/session` / `/pi-session`               | 打开当前会话查看器。仅限 DM。                                                                 |
| `/new` / `/pi-new`                                   | 重置当前会话。仅限 DM。                                                                       |
| `/model` / `/pi-model [provider/model[:thinking]]`   | 显示或切换当前对话模型。思考级别：`off`、`minimal`、`low`、`medium`、`high`、`xhigh`、`max`。 |
| `/auto-reply` / `/pi-auto-reply on\|off\|status`     | 控制当前群组/频道中的自动回复。                                                               |
| `stop` / `/stop`                                     | 停止匹配的活动运行。在 Slack 上，优先使用文本形式，以保持话题本地路由准确。                   |
| `/admin` / `/pi-admin`                               | 打开有效期 30 分钟的管理 portal。                                                             |
| `/extensions` / `/pi-extensions`                     | 列出全局和对话扩展、版本及其提供的技能。                                                      |
| `/sandbox` / `/pi-sandbox`                           | 显示当前沙箱限制，以及本办公室的门禁策略和工作区布局。                                        |
| `/pi-sandbox boost`                                  | 应用配置的 boost 限制，直到管理的沙箱运行时停止。                                             |
| `/pi-sandbox door`                                   | 报告办公室的生效门禁策略和布局，并列出可用取值。                                              |
| `/pi-sandbox door <default\|isolated\|shared\|full>` | 设置本办公室的门禁策略。参阅下方[办公室门禁策略](#办公室门禁策略)。                           |

`/pi-sandbox` 仅适用于受管理的沙箱运行时：在 `host` 或 `container:<name>` 模式下，它会回复该命令需要 `image:*`。

### 办公室门禁策略

`/pi-sandbox door` 写入该对话自己的门禁策略覆盖值。同一设置也可以在管理 portal 中按办公室设置，或设为全局默认值。

| 取值       | 效果                                                                 |
| ---------- | -------------------------------------------------------------------- |
| `default`  | 清除覆盖值，改为遵循全局 `sandbox.workspace` 设置。                  |
| `isolated` | 仅挂载本办公室自己的目录。                                           |
| `shared`   | 受信任门禁：办公室外加工作区级的 `MEMORY.md`、`skills/`、`events/`。 |
| `full`     | 受信任门禁：挂载整个工作区。                                         |

更改在下一条消息时生效，届时沙箱运行时会带着新的挂载重建；已有的容器内容会被保留。对话正忙时切换会被拒绝。底层的 `sandbox.workspace` 字段请参阅[配置](/zh-cn/configuration/)。

<Aside type="note" title="平台注册命令">
  原生 slash commands 也必须在平台中注册。命令清单位于 `src/commands/manifest.ts`：Discord 的
  application commands 与 Telegram 的命令菜单在启动时由它派生，Slack 的路由也识别清单中的 `/pi-*`
  名称。由于 Discord 会拒绝长度超过 100 个字符的选项描述，清单中的参数描述会由命令测试做长度校验。
  Slack 的 App manifest（`/pi-login`、`/pi-session`、`/pi-new`、`/pi-model`、`/pi-auto-reply`、
  `/pi-sandbox`、`/pi-extensions`、`/pi-admin`）仍需在 Slack App 配置中手动更新。
</Aside>

## 共享 vault 命令

所有 vault 命令都仅限 DM：

| 命令                          | 效果                                                   |
| ----------------------------- | ------------------------------------------------------ |
| `/login shared list`          | 列出共享登录配置文件。                                 |
| `/login shared create <name>` | 打开创建共享配置文件的登录链接。                       |
| `/login shared update <name>` | 打开更新共享配置文件的登录链接。                       |
| `/login shared delete <name>` | 删除共享配置文件。                                     |
| `/login copy <name>`          | 将共享配置文件复制到当前对话 vault。匹配的值会被覆盖。 |

共享配置文件是由运维人员管理的凭证。通过 `sandbox.defaultSharedVault` 自动继承仅限符合条件、基于成员身份信任的 image/Cloudflare 对话；GitHub 等开放触发表面不会继承它。

## Web portals

管理、登录/vault 和会话 portal 使用同一个 HTTP 服务器。生产环境请配置公开 base URL：

<Steps>

1. 设置外部可访问的 URL：

   ```bash
   export LINK_URL="https://mikan.example.com"
   ```

2. 可选设置监听端口；存在 `LINK_URL` 时默认为 `8181`：

   ```bash
   export LINK_PORT=8181
   ```

3. 仅本地测试时，只设置 `LINK_PORT`。mikan 绑定到 `127.0.0.1`，并返回
   `http://localhost:<port>` 下的链接。

</Steps>

Capability URL 是 bearer secret。不要将其粘贴到共享频道、日志或 issue tracker 中。登录链接 15 分钟后过期，并在写入凭证时被使用；管理链接有效期为 30 分钟；会话链接有效期为 24 小时。进程重启会使所有未过期链接失效。

有关路由级权限，请参阅 [Portal 身份验证和 capability 模型](/zh-cn/portal-auth-model/)。

## OAuth flows

- [GitHub](/zh-cn/oauth/github/) <Badge text="OAuth" variant="default" />
- [Google Workspace](/zh-cn/oauth/google-workspace/) <Badge text="OAuth" variant="default" />
- [Google Cloud SDK / gcloud](/zh-cn/oauth/google-cloud-sdk/) <Badge text="OAuth" variant="default" />
