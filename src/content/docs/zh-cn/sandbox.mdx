---
title: Sandbox
---

import { Aside, Badge, Card, CardGrid, LinkCard } from "@astrojs/starlight/components";

<CardGrid>
  <Card title="本地开发" icon="setting">
    `host` 最少设置、不注入 vault env。所有办公室共用同一个文件系统，无法强制 private office 的
    visibility，属于受信任部署。
  </Card>
  <Card title="主线隔离" icon="rocket">
    `image:<image>` 由 mikan 管理 lifecycle、workspace mount、vault env 与资源限制。
  </Card>
</CardGrid>

<Aside type="note" title="sandbox 的定位">
  `image:*` 面向共用一台 VM 的团队。每个 office 有自己的资源限制、文件系统隔离与独立环境。它不是 E2B
  或 SaaS sandbox 那种对抗恶意租户的强隔离：container 共用宿主机 kernel，VM
  上的人彼此互信。凭证会进入 sandbox，只是因为开发工作（`gh`、`gcloud`、`gws`）需要它们。
</Aside>

<Aside type="caution" title="模式名称">
  `docker:*` 不是可用模式；请改用 `container:*` 或 `image:*`。
</Aside>

## 支持模式

| 模式                      | 执行位置              | Vault env injection | Vault key             | 备注                                                                         |
| ------------------------- | --------------------- | ------------------- | --------------------- | ---------------------------------------------------------------------------- |
| `host`                    | 宿主机                | 不注入              | 由平台用户派生        | 仅限本地开发；无法强制 office visibility                                     |
| `container:<name>`        | 现有 Docker container | 注入                | 由 container 名称派生 | one container one vault；多人共享同一 container 就共享该 vault               |
| `image:<image>`           | mikan 管理的 Docker   | 注入                | office key            | 当前最推荐的隔离模式；`1 conversation = 1 vault = 1 container`               |
| `cloudflare:<sandbox-id>` | Cloudflare Worker     | 注入                | office key            | 建设中；需自行部署 `@cloudflare/sandbox` bridge，host workspace 不会自动同步 |

office key 就是带版本的 `v1-<platform>-<readable-id>-<hash>` 路径段，它同时也命名该对话在 workspace 下的目录。参阅[对话办公室](#对话办公室)。

## 对话办公室

每个对话在 workspace 根目录下拥有一个目录——它的**办公室**——按 office key 命名，而不是按平台的原始对话 id。Sandbox mount 跟随该目录，因此在 runtime 内部，办公室位于 `/workspace/<office-key>`。

runtime 能看到 workspace 的哪些部分，跟随该办公室的 **visibility**（ADR 0008），由 Slack 对话类型推导：公开频道是 public；私有频道、DM、群组 DM、外部共享频道与未知类型是 private。每个办公室的挂载形状相同，只差在只读标志：

| 挂载到 `/workspace` 下的内容               | Public office | Private office |
| ------------------------------------------ | ------------- | -------------- |
| `<office-key>/`——办公室本身                | 可读写        | 可读写         |
| `MEMORY.md`、`skills/`——共享知识           | 可读写        | 只读           |
| `public/<office-key>/`——所有 public office | 只读          | 只读           |

没有任何布局会挂载 workspace 根目录。admin portal 或 `/pi-sandbox visibility private` 可把公开频道收窄为 private；不能放宽超过 Slack 的设置。只有 `image:*` 会强制执行 visibility；其他模式报告 `managedProjection: false`，private office 在这些模式下会照常服务并记录一次警告。字段语义与已退役的门禁策略设置见[配置](/zh-cn/configuration/)。

<Aside type="caution" title="visibility 不是额外的 sandbox">
  Visibility 决定哪些主机目录被投影进 runtime。它不是执行边界：无论办公室的 visibility 为何，`host`
  模式都直接在宿主机上运行工具，没有任何文件系统或进程隔离。
</Aside>

### 从原始 id 布局升级

在办公室布局之前创建的 workspace 中，目录是按原始对话 id 命名的。每次启动都会迁移它们——workspace 目录、对话 vault key，以及按对话的主机 state——并为每一次移动记录日志，因此中断的迁移会继续进行，而不会丢失某个对话。

有两种情况会让启动有意停止，而不是靠猜测：

- **无归属目录。** 启用了多个平台时，mikan 无法判断某个原始目录属于哪一个。请用 `mikan office claim <conversationId> <platform>` 指明所有者（daemon 需停止）；下次启动会执行移动。
- **冲突**，即旧版目录和 office key 目录已同时存在。这些情况会被报告以便手动合并，绝不会被覆盖。

受管理的 container 能在重命名中存活：它们的 bind 会被转换到运行中 container 的快照上，因此可写层得以保留，而不是从基础镜像重建。

## 各模式文档

<LinkCard title="Host sandbox" description="直接在宿主机执行工具；最适合本地开发。" href="host/" />
<LinkCard
  title="Container sandbox"
  description="连到现有 Docker container，沿用使用者管理的 lifecycle。"
  href="container/"
/>
<LinkCard
  title="Image sandbox"
  description="由 mikan 管理 per-conversation container 与资源限制。"
  href="image/"
/>
<LinkCard />
<LinkCard />
<LinkCard
  title="Cloudflare sandbox"
  description="通过 Cloudflare Worker bridge 执行工具；建设中。"
  href="cloudflare/"
/>

## 能力差异

`image:<image>` <Badge text="推荐" variant="success" /> 是当前主要开发与推荐的 sandbox 模式；其他模式保留为本地开发、兼容、或实验用途，部分能力不会补齐。

| ------------------------------------ | -------- | ------------------ | --------------- | ------------------ | --------------- | -------------- |
| command 执行 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| mikan 管理 runtime lifecycle | 不适用 | ❌ | ✅ | ✅ mikan 进程托管 | ❌ | ❌ |
| per-conversation container / runtime | ❌ | ❌ | ✅ | ✅ | 需自行管理 | bridge 衍生 id |
| per-conversation vault env | ❌ | ❌ | ✅ | ✅ | ✅ | ✅ |
| vault file 自动投影 / bind mount | ❌ | ❌ | ✅ | ✅ | ❌ | ❌ |
| workspace 自动挂载 | host | 需自行挂载 | ✅ | ✅ | 需自行挂载 | ❌ |
| 强制执行 office visibility | ❌ | ❌ | ✅ | ✅ | ❌ | ❌ |
| idle auto-stop / recreate | 不适用 | ❌ | ✅ | ✅ | ❌ | ❌ |
| CPU / memory default limits | ❌ | ❌ | ✅ | ✅ | ❌ | ❌ |
| `/pi-sandbox boost` | ❌ | ❌ | ✅ | ✅ | ❌ | ❌ |
| agent `sandbox` tool 设置 limits | ❌ | ❌ | ✅ | ✅ | ❌ | ❌ |
| 推荐程度 | 本地开发 | legacy / 兼容 | 主线 | preview | alpha | 建设中 |

上表中大多数 ❌ 都是拒绝，而非静默降级。例外是 office visibility：无法强制执行的模式会针对每个办公室记录一次 `Sandbox '<type>' cannot enforce private office visibility` 并继续，因为这些模式本身就是受信任部署。无法 mount vault file 的模式会抛出 `Sandbox type "<type>" does not support vault file mounts`，而不是在缺少凭证的情况下运行。在无法投影文件的模式上，请只用 `env` 保存 vault 凭证。
